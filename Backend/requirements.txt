flask==3.1.2
flask-cors==6.0.2
flask-limiter==4.1.1
# Only actually used if RATE_LIMIT_STORAGE_URI/REDIS_URL is set to a
# redis:// URI (see app.py) -- flask-limiter/limits raises a
# ConfigurationError at startup without it if that's configured. Harmless
# and unused with the default "memory://" storage.
redis==8.1.0
requests==2.32.5
beautifulsoup4==4.14.3
pypdf==6.5.0
numpy==2.4.6
gunicorn==26.1.0
# Error monitoring, gated on SENTRY_DSN (see app.py) -- a no-op with it
# unset, so this is safe to ship before Aarush creates a Sentry account.
sentry-sdk[flask]==2.69.1
